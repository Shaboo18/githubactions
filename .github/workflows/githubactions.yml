name: first-action

on:
  workflow-dispatch:
    inputs:
      environment:
        description: "fksdfd"
        required: true
        default: "sdf"
        options:
          - staging
          - dfdf

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "itis working"

      - name: second-step
        run: echo "dfwjdf"

      - name: third-step
        run: echo "تعبت وربناا"
